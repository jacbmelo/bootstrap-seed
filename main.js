'use strict'

$(function() {
    
    $('header').load('_header.html');
    $('footer').load('_footer.html');

    /*
    // Load data from a json file
    $.ajax({ 
        type: 'GET', 
        url: 'data.json', 
        async: false, 
        beforeSend: function() {},
        dataType: 'json', 
        success: function(data) {
            $.each(data.navbarItems, function(index, item) {
                // Do something with data
            });
        }
    });
    */
});